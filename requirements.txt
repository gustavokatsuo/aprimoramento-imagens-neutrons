# Dependências diretas do pipeline (src/). As versões são as usadas nos testes.
#
# ATENÇÃO — instalação em cluster com GPU:
# torch e torchvision precisam do build correspondente ao runtime CUDA do nó.
# Instalar pelo índice padrão do PyPI pode trazer um build incompatível com os
# drivers. Confirme a versão de CUDA disponível (nvidia-smi, ou pergunte ao
# suporte) e instale a partir do índice correspondente, por exemplo:
#
#   pip install torch==2.13.0 torchvision==0.28.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# As bibliotecas CUDA (nvidia-*, triton) vêm como dependências desses pacotes e
# NÃO devem ser fixadas aqui: fixá-las amarra o projeto ao runtime de uma
# máquina específica e quebra a instalação em qualquer outra.

torch==2.13.0
torchvision==0.28.0
torchmetrics==1.9.0

numpy==2.4.4
pillow==12.2.0

# Leitura científica de TIFF 16-bit (data_loader.load_image_as_array)
tifffile==2026.3.3

# Opcional: só é exigida se houver arquivos .fits no conjunto de dados.
# data_loader importa astropy de forma condicional e funciona sem ela.
astropy==8.0.1

# Para abrir notebooks/arquitetura_ic.ipynb (protótipo histórico, não é o
# caminho de treino), acrescente à parte:  pip install jupyter matplotlib
